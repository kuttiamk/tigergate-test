openapi: 3.0.0
info:
  title: Vulnerable API
  version: 1.0.0
paths:
  /users/pii:
    get:
      summary: Retrieves all PII records
      # Missing security requirements (No Auth)
      responses:
        '200':
          description: OK
